// Replace with your Stripe Payment Link once you set up billing.
// Append ?client_reference_id=<user_id> at runtime (see GoProModal) so the
// webhook can map the payment back to the right account.
export const STRIPE_PRO = 'https://buy.stripe.com/14A6oAc6Qcwk7mn6rd7EQ0a'
